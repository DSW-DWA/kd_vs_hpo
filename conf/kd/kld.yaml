checkpoint_dir: checkpoints/permutations_2
log_dir: logs/permutations_2

flops_counter_mode: SILENT
flops_budget: 1.0e20
run_name: ""

optimizer_params:
  lr: 0.1
  momentum: ${general.momentum}
  weight_decay: 5e-4

scheduler_params:
  T_max: ${general.max_epochs}

student: null
teachers_mapping: null
kd_loss: 
  _target_: kd_vs_hpo.kd.losses.KullbackLeiblerKDLoss
  alpha: 0.5
  temperature: 4
